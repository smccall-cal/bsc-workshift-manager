%h1 Login

= form_tag users_path, :method => :get, :id => 'login_form' do |f|
  = select_tag "username", options_from_collection_for_select(@users, "username", "username")
  = text_field_tag "password"
  = submit_tag "Login", :id => 'login_submit'
