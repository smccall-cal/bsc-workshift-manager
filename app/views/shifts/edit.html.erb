<h1>Editing Shift</h1>
<h2>Editing of other details should be done on the weekly shift page</h2>


<%= form_tag semester_shift_path, :method => "patch" do %>

  <%= label :shift, :is_checked_off, 'Is checked off?'%>
  <%= select_tag 'shift[is_checked_off]', options_for_select(["Yes", "No"]) %>
  
  <%= label :shift, :assigned_user, 'Assigned_user'%>
  <%= select_tag 'shift[user_id]', options_for_select(@users) %>
  
  <%= label :shift, :date, 'Date of shift'%>
  <%= date_select("shift", "date")%>

  <%= submit_tag 'Save Shift'%>
  
<% end %>

<%= link_to 'Back', semester_path(@semester) %> 